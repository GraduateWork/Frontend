const prefix = '[Events]';

export const GET_EVENTS_STARTED = `${prefix} Get Events Started`;
export const GET_EVENTS_DONE = `${prefix} Get Events Done`;
export const UPDATE_FAVORITE = `${prefix} Update Favorite`;
